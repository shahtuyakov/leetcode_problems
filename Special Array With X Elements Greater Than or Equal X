/**
 * @param {number[]} nums
 * @return {number}
 */
var specialArray = function(nums) {
    for (let i = 1; i <= 100; i++) {
        let array = nums.filter(num => num >= i);
        if (i == array.length) return i
    }
    return -1;
};
