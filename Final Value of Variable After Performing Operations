var finalValueAfterOperations = function(operations) {
    let res = 0;
    for (let i = 0; i < operations.length; i++){
        if (~operations[i].indexOf("+")) res += 1;
        else if (~operations[i].indexOf("-")) res -= 1;
    }
    return res;
};


