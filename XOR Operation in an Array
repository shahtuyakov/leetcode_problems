/**
 * @param {number} n
 * @param {number} start
 * @return {number}
 */
var xorOperation = function(n, start) {
    let res = [];
    let num = 0;
    
    for (let i = 0; i < n; i++){
        res[i] = start + 2 * i;
        num = num ^ res[i];
    }
    return num
};
